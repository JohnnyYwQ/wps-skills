# 启动与通信：里程碑 1 验证结果

日期：2026-09-19。结论：当前启动链与匿名管道的终端测试入口已贯通，能在 Mac 打包/启动，在 Windows 独立执行，再回连查询和取证。没有修改生产源码或 Windows 已安装 Skill，没有发布、提交或推送。

## 最终快速验证

运行标识：`startup-20260919T094701-2a439561`。

- 默认配置，10 轮；20/20 个测试用例通过，0 个失败。Windows supervisor 运行约 26.10 秒。
- 纯通信：10 个用例，30 次业务载荷应答完全一致，另有每进程一次的就绪握手。覆盖 32、4096、65536 字符及中文、Unicode、转义字符。
- 真实 WPS：10/10 个 Task、60/60 次 Action 成功；10 份 XLSX 的 A1/B1 均通过独立 OOXML 检查，Task 资源清理与测试文档关闭均成功。
- Action 数来自最终 Task Response，包含创建、四个正文步骤和保存，不含测试握手或纯通信消息。此次不是 Agent 自然语言端到端测评。
- 运行中状态查询已返回正在执行的原生操作；随后 Windows 自行完成，Mac 回连取回最终证据，ZIP 与逐文件哈希校验通过。
- 简单单元格内容用例的视觉审阅为 `not_required`，不是“已人工看过”。

同轮阶段观测（毫秒，10 个样本）：

| 场景 | 阶段 | 中位数 | 最大值 |
| --- | --- | ---: | ---: |
| 纯通信夹具 | bridge 进程创建 | 5.883 | 7.576 |
| 纯通信夹具 | 就绪握手 | 169.581 | 177.223 |
| Excel 真实链路 | bridge 进程创建 | 5.438 | 6.672 |
| Excel 真实链路 | 就绪握手 | 462.708 | 553.222 |

就绪握手从进程创建返回后开始，包含尚未完成的解释器/脚本初始化及一次请求往返，不能直接称为纯脚本加载时间。纯通信夹具没有加载 Excel 原生脚本，两行场景不是两个等价方案的收益对照。WPS 应用可能已运行，不宣称 WPS 冷启动或 P95 保证。

最终证据位于仓库的 `build/runs/startup-communication/startup-20260919T094701-2a439561/`：

- `bundle/manifest.json`、`bundle/instrumentation.json`：源码版本、dirty 状态、实际打包文件哈希、测试专用插桩说明，包含 Mac 入口源码副本。
- `snapshots/snapshot-0535a1d558a440129245fa56c5caac55/windows/`：状态、输入、回执、trace、独立产物检查与 XLSX。
- `report.txt`：终端详细阶段报告。

查看：

```sh
python3 scripts/debug/startup_communication.py report \
  --run build/runs/startup-communication/startup-20260919T094701-2a439561 --detail
```

## 运行中取证与协作停止

最终验证运行：`startup-20260919T094633-6614b362`，使用专门的纯通信配置，未调用 WPS。7 个用例通过、0 个失败后响应显式停止，状态为 `stopped / operator_requested`；未开始下一用例。

同一运行保留了运行中的 partial 快照和停止后的 finalized 快照，最终逐文件与压缩包哈希通过。证明了这次运行可脱离 Mac 独立推进、回连取证和协作停止，不计作完成两小时稳定性测试。

## 早期失败与修复证据

早期记录均保留，不合并到最终版本成功率，也不因后来通过而覆盖。

| 运行 | 观察 | 处理 |
| --- | --- | --- |
| `startup-20260919T093040-373125f6` | Windows 20 个用例通过；Mac 运行中查询超时，最终取证的 launcher 哈希不一致 | 查询先解析日志 JSON，避免序列化附带对象属性；外层退出日志改由快照 ZIP 哈希覆盖 |
| `startup-20260919T093450-4942e660` | 20 个用例通过，运行中查询与最终哈希检查通过 | 保留为中间验证，不作为最终包 |
| `startup-20260919T093731-311d614d` | 45 个纯通信用例通过，第 46 个失败；同时进行的 Copy-Item 占用日志，Python 追加发生 PermissionError | 整次停止，没有重试。修复取证端文件共享；记录失败仍必须释放所属进程 |
| `startup-20260919T094438-cf08e899` | 扩展取证脚本超过 Windows 远程命令行长度限制 | 改用短 bootstrap + UTF-8 stdin 传脚本；发出停止请求，随后成功取回证据 |

文件共享问题有独立的最小 Windows 回归 `tests/snapshot_sharing.py`，无 WPS 或用户文档：

- 旧 Copy-Item 路径：42 次追加成功，48 次共享冲突，复制本身退出码为 0。
- 仅改变读取共享方式为 `ReadWrite | Delete` 后：215 次追加成功，0 次共享冲突，复制退出码为 0。
- 次数随运行时间变化；断言是复制成功、发生过并发追加、共享冲突为 0。随后重新跑原来的运行中取证场景通过。

因此该失败属于测试取证工具干扰执行，不是匿名管道消息传输失败。有关记录位于失败运行的 `host-logs/diagnostic.sharing-*` 和原始用例 stderr。

## 本地回归与限制

本地 15/15 项测试通过，覆盖配置时限、包篡改、打包不修改生产源码、失败即停、不重放、看门狗、协作停止、日志故障下清理、独立文档检查、快照路径和证据哈希边界等。Windows 最小文件共享回归通过。Python 编译检查通过。

尚未完成、也未宣称完成：

- 两小时持续稳定性测量、跨机器统计或长期可靠性保证。
- 简化启动链与基线的对照，以及命名管道/TCP 候选实现和比较。
- 响应丢失、乱序、截断、宿主异常退出等完整故障注入矩阵。
- Word/PPT 实机覆盖、Action 内部深入拆分、Agent 自然语言端到端调试。
- 计划任务排队、解释器自身启动等阶段的更精确拆分。

下一里程碑先固定当前通信方式，比较现有与简化启动链；本报告没有方案优劣结论。
