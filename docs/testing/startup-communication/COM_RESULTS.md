# COM 环境验收结果（2026-09-19）

本次新增诊断仅进入独立测试包，生产连接逻辑及已安装 Skill 未替换。

## 实机验收

运行：`startup-20260919T101730-d053a824`。
本地证据：`build/runs/startup-communication/startup-20260919T101730-d053a824/`。
最终快照：`snapshots/snapshot-6d5c553ecfd541888329612c173719e1/windows/`，取回时已核对文件清单和归档哈希。

| 用例 | 结果 | 用例总耗时 | 实际连接 |
| --- | --- | --- | --- |
| 新建工作簿，读写、保存、独立回读 | 通过 | 3.521 秒 | 已有实例 |
| 校验前置产物哈希，复制后重新打开，修改、保存、独立回读 | 通过 | 2.622 秒 | 已有实例 |

两个用例的环境、注册发现、获取已有实例、对应文档绑定、写入、回读、独立 XLSX 检查均成功，诊断完整性检查通过；最后只关闭已验证的测试文档。

观察环境：Windows PowerShell 5.1.26100.9444，64 位，Session 3，非管理员，STA；WPS COM 自报 Version 12.0。原始证据包含解释器、操作系统、注册表视图和异常字段。

两次都没有进入 COM 激活分支，报告明确标记 `not_executed`。未证明 WPS 冷启动成功，也未验证另一台 Windows、Word/PPT 或长期稳定性。上述耗时包含新增诊断开销，不能与旧版无 COM 插桩基线直接比较。

## 诊断故障测试

Windows 原生脚本 `tests/com_diagnostics_test.ps1` 的五项检查全部通过：不存在的 ProgID 获取失败、激活失败、注入拒绝访问后原异常继续抛出且保留 `0x80070005`、成功返回值保持不变、注册拒绝结果保持不变。

证据位于同一运行目录的 `native-negative-01/com-*.jsonl`；脚本最终回执位于 `host-logs/com.native_negative-*.stdout.txt`。该辅助测试通过 SSH 执行，未做真实 WPS 文档操作，不计入交互桌面 WPS 通过率。

本地 Python 单元测试共 22 项通过，包括未覆盖分支、失败回退、未知状态、缺失/截断证据、产物检查失败与前置条件顺序。故障注入验证的是诊断行为，不能代替真实权限配置故障或多机器稳定性测试。

## 关闭 WPS 后的激活验收

用户关闭 WPS 进程，并在测试前的进程检查中确认未发现 WPS 后，启动全新运行 `startup-20260919T102538-e6cc2a04`。
最终快照：`build/runs/startup-communication/startup-20260919T102538-e6cc2a04/snapshots/snapshot-c50568500328465a9403ffd63197cece/windows/`，已核对哈希。

两个用例均通过：创建用例 5.215 秒，重新打开用例 4.136 秒（均为整个用例耗时）。两次 `GetActiveObject` 均返回 `0x800401E3 / MK_E_UNAVAILABLE`，随后 COM 激活成功；对应文档绑定、写入、回读、独立 XLSX 验证和测试文档关闭全部成功。获取失败的原始异常链完整保留，属于预期回退路径，不是整次测试失败。

因此本机目前已实测覆盖“连接已有实例”和“获取不可用后执行激活”两条路径。第二次也进入激活只能说明当时没有取得活动 COM 对象，不能据此判断第一个 WPS 进程是否已退出。没有记录 WPS PID 创建时间与完整进程生命周期，`coldStartProven` 仍为 false；本轮不是多机器或长期稳定性证明。
