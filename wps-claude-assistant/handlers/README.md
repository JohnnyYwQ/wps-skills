common-handler.js, excel-handler.js, ppt-handler.js, word-handler.js
macOS WPS 加载项各应用处理器实现目录。
`word-handler.js` 导出 `getDocumentParagraphs`、`findInDocument`、`smartFillField` 和 `replaceBookmarkContent`，与主加载项的 Word 动作标识保持一致；实际 WPS 能力仍需按平台验收。
一旦这里的结构发生变化，请务必更新我... 就像重新标记领地一样。
