main.js, manifest.xml, ribbon.xml, wps-auto.sh, handlers/, utils/
macOS WPS 加载项与处理器入口目录。
Word 模板动作 `getDocumentParagraphs`、`findInDocument`、`smartFillField` 和 `replaceBookmarkContent` 已覆盖主分发器与 Word 处理器代码路径；实际 WPS 能力仍需按平台验收。
一旦这里的结构发生变化，请务必更新我... 就像重新标记领地一样。
